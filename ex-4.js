/// My answer 
// 4.1 Type : Arrow function
let calculator = {
  add: (a, b) => a + b,
  subtract: (a, b) => a - b,
  multiply: (a, b) => a * b,
  divide: (a, b) => a / b,
};
// 4.2
let addResult = calculator.add(10, 20);
console.log(addResult);
// 4.3
let divideResult = calculator.divide(3000, 10);
console.log(divideResult);

